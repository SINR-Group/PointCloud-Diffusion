[tool.poetry]
name = "pointcloud-diffusion"
version = "0.1.0"
description = ""
authors = ["Ahad Imran imran.ah@northeastern.edu"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.11"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
